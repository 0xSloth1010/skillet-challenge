import { useAppSelector } from 'store/store'

export const use{{pascalCase featureName}} = () => {
  const {{featureName}}State = useAppSelector(({ {{featureName}} }) => {{featureName}})

  return {
    ...{{featureName}}State,
  }
}
