import { createSlice } from '@reduxjs/toolkit'

import { State } from './types'

const initialState: State = {}

const {{featureName}}Slice = createSlice({
  name: '{{featureName}}',
  initialState,
  reducers: {},
  extraReducers: () => {},
})

export const { reducer, actions } = {{featureName}}Slice

export default {{featureName}}Slice
